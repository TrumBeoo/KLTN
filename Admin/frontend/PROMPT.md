Read and strictly follow STRIPE_DESIGN.md (Stripe style).

Refactor admin dashboard UI.

Requirements:
- Keep ALL logic and data unchanged
- Only refactor UI

Design rules:
- Use structured layout (sidebar + header + content)
- Focus on data clarity and usability
- Use professional color palette
- Apply 8px spacing system

Layout:
- Sidebar navigation
- Top header (search, notification, profile)
- Main content with sections

Components:
- KPI cards (users, revenue, rooms)
- Data tables (tenants, rooms, payments)
- Forms and modals

UX rules:
- Prioritize readability and clarity
- Make actions obvious
- Support heavy data interaction

Output:
- Professional admin dashboard

Improve UI to match Stripe-level dashboard experience.

Focus on:
- Better table readability
- Clear section separation
- Improved typography hierarchy
- Consistent spacing and alignment

Enhance UX:
- Highlight key metrics
- Improve scanning experience
- Optimize filter/sort usability

Do NOT change logic.

Goal:
- Clean, structured, scalable admin UI